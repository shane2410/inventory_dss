Django==3.2.24
pandas>=2.0
numpy>=1.25
openpyxl>=3.0
statsmodels>=0.14
scipy>=1.10
pymongo[srv]==4.6.0
# Optional (only needed if using MySQL backend)
# mysqlclient>=2.1

# Production / Render dependencies
gunicorn>=20.1.0
whitenoise>=6.0
dj-database-url>=2.1
psycopg2-binary>=2.9

# Optional MySQL driver (uncomment if you use USE_MYSQL=1)
# mysqlclient>=2.1

# MongoDB (Djongo uses PyMongo under the hood)
djongo>=1.3.6
# Djongo 1.3.x is compatible with PyMongo 3.12.x, not 4.x.
pymongo>=3.12.3,<4.0
