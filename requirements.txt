Django==6.0.4
pandas>=2.0
numpy>=1.25
openpyxl>=3.0
statsmodels>=0.14
scipy>=1.10

# Optional (only needed if using MySQL backend)
# mysqlclient>=2.1

# Production / Render dependencies
gunicorn>=20.1.0
whitenoise>=6.0

# Optional database packages (uncomment as needed)
# dj-database-url>=1.0  # For PostgreSQL on Render paid tier
# psycopg2-binary>=2.9  # PostgreSQL driver
# mysqlclient>=2.1      # MySQL driver (if USE_MYSQL=1)
